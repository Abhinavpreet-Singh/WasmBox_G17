# WasmBox frontend

React dashboard for the plugin sandbox. Mirrors StreamForge layout patterns.

```bash
npm install
npm run dev
```

Runs on **http://localhost:5174** (proxies API to `:8001`).

See [STRUCTURE.md](STRUCTURE.md) and [../WasmBox-PROJECT.md](../WasmBox-PROJECT.md).
